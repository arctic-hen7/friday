# node_modules is reinstalled in the `deps` stage from bun.lock.
node_modules

# Local sqlite state — the production container uses the `orchestrator-data`
# named volume instead.
data

# Build / cache artefacts.
dist
build
out
.cache
*.tsbuildinfo

# Logs.
*.log
logs

# Secrets / editor / OS noise.
.env
.env.*
.git
.gitignore
.idea
.vscode
.DS_Store

# Docs aren't shipped.
README.md
*.md

Dockerfile
.dockerignore
